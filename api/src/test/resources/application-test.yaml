# A profile rather than an application.yaml in src/test/resources: an identically
# named file would shadow the main configuration entirely, silently dropping
# settings such as ddl-auto: validate that these tests exist to exercise.
spring:
  datasource:
    url: jdbc:h2:mem:usermanagement-test;DB_CLOSE_DELAY=-1
